# Diagnóstico da atualização Rust v0.2.0

1. Reproduzir a resolução de dependências no diretório do wrapper e comparar
   Cargo.toml, Cargo.lock, fontes extraídas e vendor.tar.xz.
2. Corrigir somente as inconsistências demonstradas, gerando o lockfile e o
   arquivo vendor com Cargo quando necessário; alinhar o requisito de Rust.
3. Validar resolução offline com --locked, compilação Rust e testes do pacote R.
   Registrar resultados e limitações. Não fazer commit, merge ou release.

Estado inicial: manifest e lock apontam para enderecobr_rs 0.2.0; fontes vendorizadas
também contêm essa versão. Há diretórios não rastreados preexistentes, incluindo
src/vendor. A resolução a partir da raiz não lê src/.cargo/config.toml; reproduzir
a partir de src/rust antes de concluir que o lockfile está inconsistente.

Resultado: resolução offline e dois builds Rust limpos passaram. Requisito Rust
alinhado para 1.81; lock e vendor preservados. Testes R bloqueados durante a
compilação por bloqueios de arquivo do Windows, inclusive após nova tentativa.
Diagnóstico e limites em quality_reports/2026-09-08-cargo-v020.md.

Adendo após receber o erro original de `devtools::load_all()`: o `cargo build
--lib` passa, mas `cargo run --bin document` precisa vincular um executável e
tenta localizar `x86_64-w64-mingw32-gcc`, nome que não existe no Rtools 4.5
instalado. Corrigir o template `Makevars.win.in` para fornecer explicitamente
o linker do R (`$(CC)`) ao Cargo e manter flags/perfil idênticos nos dois
comandos; regenerar `Makevars.win` via `tools/config.R` e testar `load_all()`.
